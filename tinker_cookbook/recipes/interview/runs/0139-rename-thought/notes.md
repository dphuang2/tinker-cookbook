# 0139 rename-thought

**Diff**: tool name `checkpoint` → `thought`.

**Result**: accuracy **0.862**, cadence 18% skip, 82% use tool. Heavy use.

**Status**: `variance`. Parity acc, much higher tool use.

**Action**: revert to "checkpoint".
